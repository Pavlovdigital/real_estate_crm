# Flask specific variables
FLASK_APP=run.py
FLASK_DEBUG=True 
# FLASK_ENV=development # Uncomment for development mode features

# Secret key for session management and other security features
SECRET_KEY="your_very_secret_flask_key_here_please_change_me"

# CSRF Secret Key (for Flask-WTF)
WTF_CSRF_SECRET_KEY="your_separate_csrf_secret_key_here"

# Database URL
# Defines the connection string for the database.
# If this is not set, the application will fall back to a local SQLite database (site.db).
# 
# For PostgreSQL:
# DATABASE_URL="postgresql://your_db_user:your_db_password@your_db_host:your_db_port/your_db_name"
# Example for a local PostgreSQL server:
# DATABASE_URL="postgresql://crmuser:crmpassword@localhost:5432/crm_database"
#
# For SQLite (fallback, path defined in config.py):
# (No need to set DATABASE_URL if you want to use the default SQLite fallback)
# SQLITE_FALLBACK_PATH_INFO="Default is sqlite:///site.db in the project root"

# Optional: Scraper specific configurations (if any planned for .env)
# OLX_BASE_URL="https://www.olx.kz/..."
# KRISHA_BASE_URL="https://krisha.kz/..."

# Note: For actual deployment, create a .env file (DO NOT COMMIT .env) 
# and copy these variables, setting appropriate values.
# For Vercel, these should be set as Environment Variables in the project settings.
